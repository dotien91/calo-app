# Resolve react_native_pods.rb with node to allow for hoisting
require Pod::Executable.execute_command('node', ['-p',
  'require.resolve(
    "react-native/scripts/react_native_pods.rb",
    {paths: [process.argv[1]]},
  )', __dir__]).strip

platform :ios, '15.1' # Nâng version tối thiểu lên 15.1 ngay từ đầu
prepare_react_native_project!

linkage = ENV['USE_FRAMEWORKS']
if linkage != nil
  Pod::UI.puts "Configuring Pod with #{linkage}ally linked Frameworks".green
  use_frameworks! :linkage => linkage.to_sym
end

target 'ieltshunter' do
  config = use_native_modules!

  # Firebase Pods với Modular Headers để tránh lỗi xung đột với Swift/New Arch
  pod 'GoogleUtilities', :modular_headers => true
  pod 'nanopb', :modular_headers => true
  pod 'GoogleDataTransport', :modular_headers => true
  pod 'FirebaseCore', :modular_headers => true
  pod 'FirebaseInstallations', :modular_headers => true

  use_react_native!(
    :path => config[:reactNativePath],
    :hermes_enabled => true,
    :new_arch_enabled => true, # BẬT NEW ARCHITECTURE
    :app_path => "#{Pod::Config.instance.installation_root}/.."
  )

  target 'ieltshunterTests' do
    inherit! :complete
  end

  post_install do |installer|
    # React Native standard post install
    react_native_post_install(
      installer,
      config[:reactNativePath],
      :mac_catalyst_enabled => false
    )
  
    installer.pods_project.targets.each do |target|
      target.build_configurations.each do |config|
        # 1. Đồng bộ Deployment Target cho tất cả các Pods (Sửa lỗi appTransactionID)
        config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.1'
        
        # 2. Đảm bảo Swift 5 cho các module Nitro/IAP
        config.build_settings['SWIFT_VERSION'] = '5.0'
        
        # 3. Cấu hình cho StoreKit 2 và New Arch compatibility
        config.build_settings['OTHER_SWIFT_FLAGS'] ||= ['$(inherited)']
        if target.name == 'react-native-iap'
          config.build_settings['OTHER_SWIFT_FLAGS'] << '-DSTOREKIT2_ENABLED'
        end
      end
    end
  end
end
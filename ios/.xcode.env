# 1. Load NVM (Node Version Manager)
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# 2. Gán đường dẫn Node hiện tại vào biến
export NODE_BINARY=$(nvm which current)

# Bây giờ bạn có thể dùng $NODE_BINARY
echo "Node path is: $NODE_BINARY"